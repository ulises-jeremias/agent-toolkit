# Archived schemas

Historical schema files kept for reference only. They are **not** used by CI or the V CLI.

| File | Notes |
|------|-------|
| `skill.schema.json` | Validated removed `skill.json` manifests. Skills now use SKILL.md frontmatter (`schemas/skill-md-frontmatter.schema.json`). See [MIGRATION.md](../MIGRATION.md). |
