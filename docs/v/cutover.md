# V-default cutover

**Issue:** [#555](https://github.com/ulises-jeremias/agent-toolkit/issues/555)  
**ADR:** [ADR-012](../adrs/ADR-012-python-v-coexistence.md)

V is the **canonical implementation** of `agent-toolkit`. PyPI/`uvx` runs a **thin launcher** over the bundled V binary ([ADR-021](../adrs/ADR-021-pypi-binary.md) / [#535](https://github.com/ulises-jeremias/agent-toolkit/issues/535)). There is no Python CLI fallback ([python-fallback.md](python-fallback.md)).

## What changed

| Surface | Canonical | Notes |
|---------|-----------|--------|
| From-source CLI | V (`./make.vsh build-cli` / `./make.vsh install-cli`) | Installs `<prefix>/bin/agent-toolkit` (default `~/.local/bin`) |
| `doctor --json` / `version --json` | engine=`v`, version, commit | Human stdout unchanged (no engine/commit pollution) |
| Consumer + PORT/REDESIGN advanced | V | install lifecycle, skills/mcp/plugin, workspace/memory/project/loop/swarm/devcompanion |
| `insights` (DEPRECATE) / `release` (REMOVE) | disposition help / exit in V | Not ported; Python fallback removed |
| PyPI `uvx` / `uv tool install` | V binary via thin launcher (ADR-021) | npm-style trampoline only |
| GitHub Release binaries | Stable native V since `v1.11.0` | See [rollback](rollback.md). Do not retag empty `v1.10.0`. |

## Python CLI quarantine

Removed — see [python-fallback.md](python-fallback.md).

[#560](https://github.com/ulises-jeremias/agent-toolkit/issues/560) left `insights` as **DEPRECATE** and `release` as **REMOVE** — not ported to V. Product command:

```bash
uvx --from agent-toolkit-cli agent-toolkit doctor
```

Do **not** set an engine env var (ADR-012 rejected option B).

## 9163c93 Drop (2026-08-14) — Python swarm deleted (14 files), trampoline kept

At `fbb2280` (`2026-08-12 fix(ci): always tag Docker images`) `packages/pypi` still contained 95 files incl `launcher.py` and the full `agent_toolkit/swarm` Python implementation (old prefix `packages/agent-toolkit-cli/src/agent_toolkit/swarm` before `2fbb503 pypi layout` rename → new `modules/agent_toolkit_core/`). At `9163c93` (`2026-08-14 01:20:52 -0300 chore(pypi): drop Python CLI quarantine; keep npm-style V trampoline`) the swarm was deleted:

```bash
git show --stat 9163c93 | head -n 40
# 14 packages/pypi/.../swarm files + 33 cli/* files deleted
# kept: packages/pypi/agent-toolkit-cli/hatch_build.py + src/agent_toolkit/launcher.py trampoline
```

`launcher.py` no longer imports a Python CLI — it prefers the **V binary**:

- `make.vsh:160` `build-cli` compiles V:
  `vcmd('-d commit=${commit} -o ${out} ${join_path(r, "cmd/agent-toolkit")}')` + `cp build/agent-toolkit-v build/agent-toolkit`
- Resolution order (see [pypi-launcher.md](pypi-launcher.md)): `AGENT_TOOLKIT_BIN` → `agent_toolkit/bin/agent-toolkit` inside the wheel → `AGENT_TOOLKIT_ROOT/build/agent-toolkit{,-v}` (dev fallback `AGENT_TOOLKIT_ROOT=$PWD`)
- `VMODULES=$(pwd)/modules` vs `embedded_data` bundling (`modules/agent_toolkit_core/embedded_data.v` generated by `scripts/generate-embedded-data.vsh` via `gen-embedded`) — two tiers documented in `docs/HOW_TO_DEVELOP_V.md` and `distribution/README.md`

`.v-version` pin is `0.5.2` per `docs/HOW_TO_DEVELOP_V.md` (`import json` not `json2` for `v 0.5.2`, `time`, etc).

CLI contract lives in `docs/compatibility/cli-contract.yaml` (not stale `parity.yml`); CI validates via `AGENTS.md` `validate-loops` and `docs/CLI_SURFACES.md` now lists `serve` (PORT, #833 `vlib/veb`) and `tui` REMOVED in 1.23.0 (ADR-030) as V-extra surfaces.

Archived narrative: [archive/cutover.md](archive/cutover.md). Rollback: [rollback.md](rollback.md).

## Local V install

```bash
./make.vsh install-cli                         # ~/.local/bin/agent-toolkit
./make.vsh install-cli --prefix=/usr/local     # /usr/local/bin/agent-toolkit
# (vlib/build skips hyphen flags; --prefix is parsed by make.vsh. PREFIX env still works.)
agent-toolkit doctor --json      # engine/version/commit
```

Parity harness still accepts `build/agent-toolkit-v` (copy of the canonical binary).

## Verification

```bash
cat .v-version  # 0.5.2
./make.vsh build-cli
AGENT_TOOLKIT_ROOT=$PWD ./build/agent-toolkit build --check
AGENT_TOOLKIT_ROOT=$PWD ./build/agent-toolkit doctor --json | grep '"engine": "v"'
grep -rn "9163c93" docs/v | head  # this file + CHANGELOG
```
