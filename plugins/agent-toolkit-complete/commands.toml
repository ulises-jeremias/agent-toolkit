[[commands]]
name = "assistant"
description = "Assistant — on any repo, scan README→docs→AGENTS→CONTRIBUTING→PR templates→task runners→devcontainer→CI→configs before c"
prompt = """
Follow the `assistant` skill instructions for {{args}}.

@{skills/assistant/SKILL.md}
"""

[[commands]]
name = "dev_companion"
description = "WHAT — Dev Companion (general): layered companion for client delivery; modes, gates, delegation to assistant and workflo"
prompt = """
Follow the `dev-companion` skill instructions for {{args}}.

@{skills/dev-companion/SKILL.md}
"""

[[commands]]
name = "onboarding"
description = "Getting started guide for new users. Walks through setup validation, the skill/agent hierarchy, and the three most usefu"
prompt = """
Follow the `onboarding` skill instructions for {{args}}.

@{skills/onboarding/SKILL.md}
"""

[[commands]]
name = "output_handshake"
description = "WHAT — Default gate for any deliverable: confirm where the final artifact will be stored and that a human will review, b"
prompt = """
Follow the `output-handshake` skill instructions for {{args}}.

@{skills/output-handshake/SKILL.md}
"""

[[commands]]
name = "pr_fallback"
description = "WHAT — When the repo has no GitHub PR template, structure the pull-request body using the default in references/pr-body-"
prompt = """
Follow the `pr-fallback` skill instructions for {{args}}.

@{skills/pr-fallback/SKILL.md}
"""

[[commands]]
name = "workspace_knowledge_sync"
description = "Syncs knowledge to the ai-workspace knowledge base. Use when the assistant discovers new patterns, learns user preferenc"
prompt = """
Follow the `workspace-knowledge-sync` skill instructions for {{args}}.

@{skills/workspace-knowledge-sync/SKILL.md}
"""

[[commands]]
name = "dbt_validation"
description = "HOW — Run dbt checks as documented in the target repo (parse, compile, test, selective run). Does not configure Snowflak"
prompt = """
Follow the `dbt-validation` skill instructions for {{args}}.

@{skills/dbt-validation/SKILL.md}
"""

[[commands]]
name = "snowflake_validation"
description = "HOW — Read-only Snowflake validation patterns: use repo-documented CLI (snowflake/sql) or SQL checks. Never claim succes"
prompt = """
Follow the `snowflake-validation` skill instructions for {{args}}.

@{skills/snowflake-validation/SKILL.md}
"""

[[commands]]
name = "adr"
description = "WHAT — Create and maintain Architecture Decision Records (ADRs) per the process. Covers when to write an ADR, required s"
prompt = """
Follow the `adr` skill instructions for {{args}}.

@{skills/adr/SKILL.md}
"""

[[commands]]
name = "agreement"
description = "WHAT - Capture explicit agreements, terms, parties involved, dates, validity, and linked work items using the Agreement"
prompt = """
Follow the `agreement` skill instructions for {{args}}.

@{skills/agreement/SKILL.md}
"""

[[commands]]
name = "bug"
description = "WHAT - Draft and review bugs using the Bug Template; classifies whether an issue should be escalated to incident based o"
prompt = """
Follow the `bug` skill instructions for {{args}}.

@{skills/bug/SKILL.md}
"""

[[commands]]
name = "decision_log"
description = "WHAT - Capture lightweight project, product, or operational decisions that do not require a full ADR; includes rationale"
prompt = """
Follow the `decision-log` skill instructions for {{args}}.

@{skills/decision-log/SKILL.md}
"""

[[commands]]
name = "development_workflow"
description = "WHAT - Default development workflow, task lifecycle, DoR, DoD, validation, and evidence model when a project has no expl"
prompt = """
Follow the `development-workflow` skill instructions for {{args}}.

@{skills/development-workflow/SKILL.md}
"""

[[commands]]
name = "epic"
description = "WHAT - Draft and review epics using the Best Practices Epic Template; includes objectives, success criteria, related tas"
prompt = """
Follow the `epic` skill instructions for {{args}}.

@{skills/epic/SKILL.md}
"""

[[commands]]
name = "incident"
description = "WHAT - Draft and review incident reports and RCA notes using Incident Management guidance; includes detection, impact, t"
prompt = """
Follow the `incident` skill instructions for {{args}}.

@{skills/incident/SKILL.md}
"""

[[commands]]
name = "management_unit_assessment"
description = "WHAT - Evidence-based management unit assessment for governance, delivery, collaboration, culture, and AI-native managem"
prompt = """
Follow the `management-unit-assessment` skill instructions for {{args}}.

@{skills/management-unit-assessment/SKILL.md}
"""

[[commands]]
name = "meeting_minutes"
description = "WHAT - Create structured meeting minutes from notes or transcripts using meeting templates, with redaction, action items"
prompt = """
Follow the `meeting-minutes` skill instructions for {{args}}.

@{skills/meeting-minutes/SKILL.md}
"""

[[commands]]
name = "planning"
description = "WHAT - Planning, estimation, task breakdown, and iteration capacity fallback based on Best Practices. Use before finaliz"
prompt = """
Follow the `planning` skill instructions for {{args}}.

@{skills/planning/SKILL.md}
"""

[[commands]]
name = "prd"
description = "WHAT — Draft and review a Product Requirements Document (PRD) using the template. Business-level requirements, acceptanc"
prompt = """
Follow the `prd` skill instructions for {{args}}.

@{skills/prd/SKILL.md}
"""

[[commands]]
name = "project_assessment"
description = "WHAT - Interactive project assessment router: define assessment scope and units, collect evidence through project-assess"
prompt = """
Follow the `project-assessment` skill instructions for {{args}}.

@{skills/project-assessment/SKILL.md}
"""

[[commands]]
name = "project_assessment_evidence"
description = "WHAT - Interactive evidence intake for project assessments. Ask the user where each evidence source lives, build an evid"
prompt = """
Follow the `project-assessment-evidence` skill instructions for {{args}}.

@{skills/project-assessment-evidence/SKILL.md}
"""

[[commands]]
name = "spike"
description = "WHAT - Produce spike and research findings using the spike template; captures purpose, findings, implementation strategy"
prompt = """
Follow the `spike` skill instructions for {{args}}.

@{skills/spike/SKILL.md}
"""

[[commands]]
name = "task"
description = "WHAT - Draft and review technical tasks using the Best Practices Task Template; includes summary, technical notes, AC, e"
prompt = """
Follow the `task` skill instructions for {{args}}.

@{skills/task/SKILL.md}
"""

[[commands]]
name = "technical_unit_assessment"
description = "WHAT - Evidence-based technical unit assessment for repositories, platforms, frontend, backend, infrastructure, data, UI"
prompt = """
Follow the `technical-unit-assessment` skill instructions for {{args}}.

@{skills/technical-unit-assessment/SKILL.md}
"""

[[commands]]
name = "trd"
description = "WHAT — Draft and review a Technical Requirements Document (TRD) using the template, typically from an agreed PRD. Covers"
prompt = """
Follow the `trd` skill instructions for {{args}}.

@{skills/trd/SKILL.md}
"""

[[commands]]
name = "user_story"
description = "WHAT - Draft and review user stories using the task template plus the As a/I want/so that format from Best Practices exa"
prompt = """
Follow the `user-story` skill instructions for {{args}}.

@{skills/user-story/SKILL.md}
"""

[[commands]]
name = "work_item"
description = "WHAT - Router for creating and refining epics, user stories, tasks, bugs, and incidents using Best Practices work item h"
prompt = """
Follow the `work-item` skill instructions for {{args}}.

@{skills/work-item/SKILL.md}
"""

[[commands]]
name = "workflow_client_bootstrap"
description = "WHAT — Interactive interview to capture client delivery context and store it inside the user's ~/.ai-workspace (or simil"
prompt = """
Follow the `workflow-client-bootstrap` skill instructions for {{args}}.

@{skills/workflow-client-bootstrap/SKILL.md}
"""

[[commands]]
name = "workflow_generic_project"
description = "WHAT — Generic client delivery: Jira or ClickUp, full repo context, human gates, English traceability on tickets, draft"
prompt = """
Follow the `workflow-generic-project` skill instructions for {{args}}.

@{skills/workflow-generic-project/SKILL.md}
"""

[[commands]]
name = "figma"
description = "Use the Figma MCP server to fetch design context, screenshots, variables, and assets, and to translate Figma nodes into"
prompt = """
Follow the `figma` skill instructions for {{args}}.

@{skills/figma/SKILL.md}
"""

[[commands]]
name = "figma_code_connect_components"
description = "Connects Figma design components to code components using Code Connect mapping tools. Use when user says 'code connect',"
prompt = """
Follow the `figma-code-connect-components` skill instructions for {{args}}.

@{skills/figma-code-connect-components/SKILL.md}
"""

[[commands]]
name = "figma_create_design_system_rules"
description = "Generates custom design system rules for the user's codebase. Use when user says 'create design system rules', 'generate"
prompt = """
Follow the `figma-create-design-system-rules` skill instructions for {{args}}.

@{skills/figma-create-design-system-rules/SKILL.md}
"""

[[commands]]
name = "figma_create_new_file"
description = "Create a new blank Figma file. Use when the user wants to create a new Figma design or FigJam file, or when you need a n"
prompt = """
Follow the `figma-create-new-file` skill instructions for {{args}}.

@{skills/figma-create-new-file/SKILL.md}
"""

[[commands]]
name = "figma_implement_design"
description = "Translates Figma designs into production-ready application code with 1:1 visual fidelity. Use when implementing UI code"
prompt = """
Follow the `figma-implement-design` skill instructions for {{args}}.

@{skills/figma-implement-design/SKILL.md}
"""

[[commands]]
name = "gh_address_comments"
description = "Triage and address open GitHub PR review and conversation comments using the gh CLI. Use when the user wants to 'address"
prompt = """
Follow the `gh-address-comments` skill instructions for {{args}}.

@{skills/gh-address-comments/SKILL.md}
"""

[[commands]]
name = "gh_contribution_planner"
description = "Daily GitHub contribution planner — analyzes the gh-logged-in user's non-archived repos (owned + forks) and recent contr"
prompt = """
Follow the `gh-contribution-planner` skill instructions for {{args}}.

@{skills/gh-contribution-planner/SKILL.md}
"""

[[commands]]
name = "gh_fix_ci"
description = "Diagnose failing GitHub Actions checks on a PR via gh, summarize the failure context, propose a plan, and only implement"
prompt = """
Follow the `gh-fix-ci` skill instructions for {{args}}.

@{skills/gh-fix-ci/SKILL.md}
"""

[[commands]]
name = "github_cli_workflow"
description = "HOW — GitHub CLI (gh): push branch, create draft PR with title/body from template or file; fallback untracked PR_DESCRIP"
prompt = """
Follow the `github-cli-workflow` skill instructions for {{args}}.

@{skills/github-cli-workflow/SKILL.md}
"""

[[commands]]
name = "gitlab_cli_workflow"
description = "HOW — GitLab CLI (glab): push branch, create draft MR with title/body; fallback untracked markdown. Use when origin is G"
prompt = """
Follow the `gitlab-cli-workflow` skill instructions for {{args}}.

@{skills/gitlab-cli-workflow/SKILL.md}
"""

[[commands]]
name = "workflow_client_bootstrap"
description = "DEPRECATED alias. Use workflow-client-bootstrap instead."
prompt = """
Follow the `workflow-client-bootstrap` skill instructions for {{args}}.

@{skills/workflow-client-bootstrap/SKILL.md}
"""

[[commands]]
name = "workflow_generic_project"
description = "DEPRECATED alias. Use workflow-generic-project instead."
prompt = """
Follow the `workflow-generic-project` skill instructions for {{args}}.

@{skills/workflow-generic-project/SKILL.md}
"""

[[commands]]
name = "clickup_cli"
description = "ClickUp CLI for managing tasks, sprints, comments, statuses, and Docs. Use when the user needs to interact with ClickUp"
prompt = """
Follow the `clickup-cli` skill instructions for {{args}}.

@{skills/clickup-cli/SKILL.md}
"""

[[commands]]
name = "linear"
description = "Manage Linear issues, projects and cycles via the Linear MCP server. Use when the user wants to read, triage, create or"
prompt = """
Follow the `linear` skill instructions for {{args}}.

@{skills/linear/SKILL.md}
"""

[[commands]]
name = "slack_assistant"
description = "Interact with Slack workspaces for reading channels/messages, sending messages, adding reactions, and browsing canvases."
prompt = """
Follow the `slack-assistant` skill instructions for {{args}}.

@{skills/slack-assistant/SKILL.md}
"""

[[commands]]
name = "slack_cli"
description = "Interact with the official Slack CLI to create, run, deploy, and manage Slack apps, environments, manifests, and trigger"
prompt = """
Follow the `slack-cli` skill instructions for {{args}}.

@{skills/slack-cli/SKILL.md}
"""

[[commands]]
name = "loop_runner"
description = "Execute and manage loop engineering primitives (init, run, status, audit) from an AI coding session via bin/loop CLI."
prompt = """
Follow the `loop-runner` skill instructions for {{args}}.

@{skills/loop-runner/SKILL.md}
"""

[[commands]]
name = "docs_generator"
description = "WHAT — Generate or update documentation from code: README.md from repo structure, CHANGELOG.md from git history, API ref"
prompt = """
Follow the `docs-generator` skill instructions for {{args}}.

@{skills/docs-generator/SKILL.md}
"""

[[commands]]
name = "llm_cost_advisor"
description = "WHAT — Recommend the most cost-effective LLM provider for a given task type. Shows estimated cost per run across availab"
prompt = """
Follow the `llm-cost-advisor` skill instructions for {{args}}.

@{skills/llm-cost-advisor/SKILL.md}
"""

[[commands]]
name = "swarm"
description = "Launch an Agent Toolkit swarm from a natural language request using agent-toolkit swarm CLI with Herdr/tmux eager window"
prompt = """
Follow the `swarm` skill instructions for {{args}}.

@{skills/swarm/SKILL.md}
"""

[[commands]]
name = "swarm_handoff"
description = "Create artifact/commit file handoffs for Agent Toolkit swarms with worktree-per-writer, branch, and promotion integratio"
prompt = """
Follow the `swarm-handoff` skill instructions for {{args}}.

@{skills/swarm-handoff/SKILL.md}
"""

[[commands]]
name = "swarm_observer"
description = "Observe, diagnose, and recover Agent Toolkit swarm runs via status, handoffs, logs, and attach with worktree and shell a"
prompt = """
Follow the `swarm-observer` skill instructions for {{args}}.

@{skills/swarm-observer/SKILL.md}
"""

[[commands]]
name = "triage"
description = "Workstation health triage — validate tooling, directory layout, and run doctor with remediation suggestions."
prompt = """
Follow the `triage` skill instructions for {{args}}.

@{skills/triage/SKILL.md}
"""

[[commands]]
name = "herdr"
description = "Manage Herdr workspaces, tabs, and panes for Agent Toolkit swarms with eager windows, shell-aware execution, and reuse."
prompt = """
Follow the `herdr` skill instructions for {{args}}.

@{skills/herdr/SKILL.md}
"""

[[commands]]
name = "inventory"
description = "Discover installed skills, agents, loops, and platform capabilities via agent-toolkit inventory, matrix, and skills list"
prompt = """
Follow the `inventory` skill instructions for {{args}}.

@{skills/inventory/SKILL.md}
"""

[[commands]]
name = "jupyter_notebook"
description = "Create, scaffold, or refactor Jupyter notebooks (.ipynb) for experiments and tutorials. Prefer the bundled templates and"
prompt = """
Follow the `jupyter-notebook` skill instructions for {{args}}.

@{skills/jupyter-notebook/SKILL.md}
"""

[[commands]]
name = "playwright_cli"
description = "Drive a real browser from the terminal using the Playwright CLI (snapshot, click, fill, screenshots, traces). Use when t"
prompt = """
Follow the `playwright-cli` skill instructions for {{args}}.

@{skills/playwright-cli/SKILL.md}
"""

[[commands]]
name = "project"
description = "Clone, index, and orchestrate multi-repo work via agent-toolkit project — symlinks, quick access, and swarm workspaces."
prompt = """
Follow the `project` skill instructions for {{args}}.

@{skills/project/SKILL.md}
"""

[[commands]]
name = "workspace"
description = "Scaffold and manage the stateless AI workspace — context, packs, repos, and knowledge for multi-repo orchestration."
prompt = """
Follow the `workspace` skill instructions for {{args}}.

@{skills/workspace/SKILL.md}
"""

[[commands]]
name = "worktree"
description = "Manage Git worktrees per writer for Agent Toolkit swarms — isolated branches, handoff promotion, and cleanup."
prompt = """
Follow the `worktree` skill instructions for {{args}}.

@{skills/worktree/SKILL.md}
"""

[[commands]]
name = "mcp"
description = "Configure and manage MCP providers for Agent Toolkit — setup, list, doctor, and per-tool deployment."
prompt = """
Follow the `mcp` skill instructions for {{args}}.

@{skills/mcp/SKILL.md}
"""

