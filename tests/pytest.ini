[pytest]
# Tooling config only — this repo is not a Python uv workspace.
# Product CLI is V (cmd/, modules/). PyPI adapter: packages/pypi/agent-toolkit-cli.
testpaths = .
addopts = -v --tb=short
pythonpath = ../packages/pypi/agent-toolkit-cli/src
